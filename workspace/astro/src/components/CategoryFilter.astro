---
const { categories = [] } = Astro.props;
---

<div class="mb-6">
  <h2 class="font-semibold mb-2">📂 Categorías</h2>
  <ul class="flex flex-wrap gap-2 text-sm">
    {categories.map(cat => (
      <li>
        <a href={`?category=${cat}`} class="px-2 py-1 border rounded hover:bg-gray-100">{cat}</a>
      </li>
    ))}
  </ul>
</div>